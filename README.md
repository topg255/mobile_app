Pour activer Azure/OpenAI: décommenter les variables .env + changer DEFAULT_AI_PROVIDER dans ai-config.service.ts.

#pour lancer l'application (backend) en serveur :
docker compose up --build 

#pour lancer le front et le build :
-> npm i
-> npm run build
